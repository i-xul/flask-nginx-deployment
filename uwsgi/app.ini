[uwsgi]
module = app.app:app

master = true
processes = 2
threads = 2

socket = /run/flask-app.sock
chmod-socket = 660
vacuum = true

die-on-term = true

uid = www-data
gid = www-data

chdir = /opt/flask-nginx-deployment
